---
name: do
category: project
description: 기존 패턴을 우선하고 새 의존성 없이 최소 변경으로 구현합니다.
command: /do
---

Use the `workflow` skill in do mode.

Implement the smallest safe change that satisfies the agreed success criteria. Prefer existing project patterns, avoid new dependencies unless explicitly requested, and keep unrelated refactors out of scope.

